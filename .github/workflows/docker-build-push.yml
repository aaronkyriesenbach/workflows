name: Docker Build and Push (reusable)

on:
  workflow_call:
    inputs:
      package-json-path:
        description: Path to the package.json used to determine the image version
        type: string
        default: package.json

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read version from package.json
        id: version
        run: echo "version=$(jq -r .version ${{ inputs.package-json-path }})" >> "$GITHUB_OUTPUT"

      - name: Generate Docker tags
        id: tags
        run: |
          IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          IMAGE=$(echo "$IMAGE" | tr '[:upper:]' '[:lower:]')

          if [[ "${{ github.ref_name }}" == "master" ]]; then
            TAGS="${IMAGE}:${{ steps.version.outputs.version }}"
            TAGS="${TAGS},${IMAGE}:latest"
          else
            SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
            TAGS="${IMAGE}:${{ steps.version.outputs.version }}-SNAPSHOT-${SHORT_SHA}"
          fi

          echo "tags=${TAGS}" >> "$GITHUB_OUTPUT"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Refuse branch build if release already exists
        if: github.ref_name != 'master'
        run: |
          IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          IMAGE=$(echo "$IMAGE" | tr '[:upper:]' '[:lower:]')
          RELEASE_TAG="${IMAGE}:${{ steps.version.outputs.version }}"

          if docker manifest inspect "$RELEASE_TAG" > /dev/null 2>&1; then
            echo "::error::Release build already exists for version ${{ steps.version.outputs.version }} ($RELEASE_TAG). Bump the version in ${{ inputs.package-json-path }} before pushing further branch builds."
            exit 1
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.tags.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
